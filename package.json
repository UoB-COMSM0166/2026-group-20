{
  "name": "my-paint-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "lint": "eslint src/*.js",
    "test": "vitest run",
    "build": "vite build",
    "run-all": "npm run lint && npm run test && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "p5": "^1.11.11"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.2",
    "globals": "^17.4.0",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
